# R3 验收证据

对应 [改造要求](README.md) 第 8 节 R3 和 [验收账本](ACCEPTANCE.md)。

本文件记录 **当前提交实际跑过的检查** 和 **明确未覆盖项**。自动化通过不等于真机、双向跨端或签名发布通过。未运行项保持「未验证」，不把旧连通性 smoke 或 mock 双端填入通过栏。

## 1. 版本

| 字段 | 值 |
| --- | --- |
| 移动 commit | 本文件所在提交（R2 实现基线 `e2a03109`） |
| 主仓对照基线 | `9b5956a7` |
| 运行服务端 | 未在本机联调；不填写生产版本 |
| 应用版本 | `0.1.0` / versionCode `1` |
| 日期 | 2026-09-17 |
| 执行环境 | Windows 开发机；Jest + Expo export；无本机真机、无本机模拟器会话 |

## 2. 本机已执行命令

在 `codex/mobile-dl-redesign-requirements` worktree：

```text
corepack pnpm check
node --test plugins/*.test.cjs
corepack pnpm export:android
git diff --check
```

`corepack pnpm build:android` / `assembleRelease` 需要 JDK、Android SDK 和签名密钥。本机未声称已产出正式签名 APK。PR 测试包路径仍是 `.github/workflows/android-test.yml`；正式签名包路径仍是 `.github/workflows/android-release.yml`。

OTA 未配置，判定 **不适用**。

## 3. 账本判定规则

| 栏 | 含义 |
| --- | --- |
| 规范 | 要求已写在改造文档 |
| 代码 | 当前分支有对应实现 |
| 自动化 | 本机 Jest／契约测试覆盖了可离线验证的不变量 |
| 判定 | 账本正式结论。需要真机、系统 IME、Web↔Android 真实往返或签名包的项目保持 **未运行** |

客户端 mock 不能证明权限、配额或跨端结果。

## 4. 功能项

| ID | 规范 | 代码 | 自动化 | 判定 | 说明 |
| --- | --- | --- | --- | --- | --- |
| A01 | 是 | 登录壳、默认 origin | 登录单测（配置包无服务器栏） | 未运行 | 缺全新安装签名包与真实 GitHub OAuth |
| A02 | 是 | 错误文案、强更门禁 | `updateDecision` 网络失败≠强更 | 未运行 | 缺真实过期／空间关闭／强更包 |
| A03 | 是 | 单层标题、详情栈 | 无端到端导航 | 未运行 | 缺真机返回栈 |
| A04 | 是 | 详情独立页、Dialog | 无 | 未运行 | 缺系统返回／键盘 |
| A05 | 是 | MessageContent／compose | 结构化 block 解析 | 未运行 | 缺 Web↔Android 真服务往返 |
| A06 | 是 | fallback | `tests/r3-acceptance.test.ts`、`tests/r2.test.ts` | 未运行 | 自动化覆盖安全摘要；缺真机恶意样本 |
| A07 | 是 | replyToMessageId | 解析保留回复字段 | 未运行 | 缺跨端引用 |
| A08 | 是 | 提及候选 | 多 @、IME `@成` 不发送 | 未运行 | 缺中文 IME 真机 |
| A09 | 是 | emotes／react | 未知 action 拒绝；反应字段解析 | 未运行 | 缺 Web 同步 |
| A10 | 是 | 长按／更多 | 无 | 未运行 | 缺滚动误触真机 |
| A11 | 是 | recall／hide／pin 分命令 | 无跨端 | 未运行 | 缺 Web 对照 |
| A12 | 是 | clientMessageId 对账 | 迟到失败不覆盖 canonical | 未运行 | 缺慢网真机 |
| A13 | 是 | 分页、回到最新 | 1000 条去重 | 未运行 | 缺滚动真机 |
| A14 | 是 | markRead 仅近底部 | 无 | 未运行 | 缺两端已读 |
| A15 | 是 | 草稿按 targetKey | 会话／话题草稿隔离 | 未运行 | 缺杀进程重开真机 |
| A16 | 是 | pendingAttachment | 选文件不是消息 | 未运行 | 缺系统选择器 |
| A17 | 是 | 既有分片传输 | `tests/transfers.test.ts` | 未运行 | 缺弱网真机 |
| A18 | 是 | 下载／分享 | 既有传输测试 | 未运行 | 缺系统分享真机 |
| A19 | 是 | 无权不可下载 | FileRow 禁用 | 未运行 | 缺真文件终态 |
| A20 | 是 | 话题列表／加入 | targetKey 隔离 | 未运行 | 缺真话题 |
| A21 | 是 | 创建话题、默认同步关 | 无 | 未运行 | 缺真群 |
| A22 | 是 | 未加入不可发 | 无 | 未运行 | 缺失权真场景 |
| A23 | 是 | 卡片动作白名单 | `assertAllowedCardAction` | 未运行 | 缺真卡片 revision |
| A24 | 是 | ReplayTracker | `tests/protocol.test.ts` | 未运行 | 缺真 WS 乱序 |
| A25 | 是 | 文件库／成员搜索文案 | 无 | 未运行 | 缺真搜索范围 |
| A26 | 是 | 资料／偏好 | `tests/settings.test.ts` | 未运行 | 缺 Web 刷新对照 |
| A27 | 是 | auto-hide | 折叠不等于撤回 | 未运行 | 缺真机展开 |
| A28 | 是 | 本地通知 | self／replay／muted／topic mention | 未运行 | 缺系统权限真机 |
| A29 | 是 | 退出清缓存 | runtime 退出测试 | 未运行 | 缺换账号真机 |
| A30 | 是 | 同一 ChatScreen | conversation／topic target | 未运行 | 缺四种会话真机 |
| A31 | 是 | 退出话题确认 | 无 | 未运行 | 缺 Web 参与状态 |
| A32 | 是 | 资料离开询问、偏好去抖 | `tests/settings.test.ts` | 未运行 | 缺返回键真机 |
| A33 | 是 | 发送后 nearBottom | 无 | 未运行 | 缺历史窗口真机 |

## 5. 视觉与辅助技术

| ID | 自动化 | 判定 | 说明 |
| --- | --- | --- | --- |
| V01–V04 | 无设备截图 | 未运行 | 工作台与页面共用组件；缺标注 dp 的 APK 截图 |
| V05 TalkBack | 控件有名称 | 未运行 | 缺真机读屏 |
| V06 200% 字体 | 热区 token 48dp | 未运行 | 缺 fontScale 真机 |
| V07 1000 条 | merge 去重单测 | 未运行 | 缺真机帧／内存 |
| V08 工作台 | `tests/ui.test.tsx` 导入正式组件 | 未运行 | 工作台通过≠真实流程通过 |

## 6. 明确限制（不冒充完成）

- 未做真机主流程、中文 IME、系统返回、后台恢复、TalkBack。
- 未做 Web↔Android 真实服务端双向对照。
- 未在本机打正式签名 APK／AAB，未计算发布 SHA-256。
- 未验证 API 26 与当前 targetSdk 的安装启动。
- OTA 未配置：**不适用**。
- 设备会话、更多主题家族、跨设备草稿、远程推送、全文搜索：不做、无占位入口。
- 表情包管理工具、头像上传、话题关闭／归档管理：未交付。

这些限制阻断把本分支标为「DL 体验改造完成」。它们不阻断记录 R3 证据本身。

## 7. 命令结果

2026-09-17 在 redesign worktree 执行。失败不得改写为通过。

| 命令 | 结果 |
| --- | --- |
| `corepack pnpm install --frozen-lockfile` | 通过 |
| `corepack pnpm check` | 通过。13 suites / 87 tests；17 份文档围栏检查 |
| `node --test plugins/*.test.cjs` | 3 通过，1 skip（Linux release tag） |
| `corepack pnpm export:android` | 通过。写出 gitignored `dist/`，Android bundle `index-2e59d598e595d81d85094d92ddffd7e1.hbc`（约 4.94 MB） |
| `git diff --check` | 通过（仅 CRLF 提示） |
| `assembleRelease` / 正式签名 APK | **未运行**。本机 `ANDROID_HOME` 为空；有 JDK 17，无 Android SDK |
| 真机／模拟器安装启动 | **未运行** |
| Web↔Android 真服务往返 | **未运行** |
| OTA | **不适用**（`updates.enabled` 未配置） |
